"use client"
import React from 'react'
import pageClients from '@/app/ui/dashboard/clients/pageClients'
const Page = ({params}) => {
  return (
    < pageClients params = { params } />
  )
}

export default Page